function sumStr(a,b) {
    if (a === "" && b === "") {
      a=0;
      b=0;
    } else if (a === "") {
      a=0
    } else if (b === "") {
      b=0
    } else {
      
    }
    return String(Number(a)+Number(b))
}